use int.Int
let rec ack (m : int) ( n : int)
    requires { m >= 0 /\ n >= 0 }
    ensures { result >= 0 }
    variant { "m, n" }
  = if m = 0 then
        n+1
    else if n = 0 then
        ack (m-1) 1
     else
        ack (m-1) (ack m (n-1))
